Autorun should, in order:
Run runServer.bat, which starts a small webserver local only
Wait some seconds for server to settle
Run launch.bat, which opens the web page the server started on (127.0.0.1:8000)